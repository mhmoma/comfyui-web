# comfyui-Web
ComfyUI Web — A portable desktop app for ComfyUI image generation. One-click launch with built-in proxy (no CORS setup needed). Features a tag-based prompt builder, SDXL/Anima support, LoRA, ControlNet, Hires Fix, img2img, and system tray mode. Just unzip and run.
