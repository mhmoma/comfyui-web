---
description: ComfyUI-Web 项目部署、Git 推送、D1 数据库操作的完整规范与凭据
globs: *
alwaysApply: true
---

# ComfyUI-Web 部署与 D1 数据库操作规范

> **部署版本**: 1.91
> **版本记录**: 见底部「版本历史」  
> **重要**: 每次修改代码后，必须更新本文件的**部署版本号**和**版本历史**。凡前端相关代码变更（`index.html` / `script.js` / `style.css` 及 `deploy/` 对应文件），必须递增前端版本号与静态资源版本参数。

---

## 1. 项目基本信息

- **项目名称**: ComfyUI Web (AI 绘图前端工具)
- **工作区路径**: `d:\桌宠\comfyui-web`
- **线上地址**: https://comfyui-web-89u.pages.dev/
- **部署平台**: Cloudflare Pages
- **数据库**: Cloudflare D1
  - 数据库名: `comfyui-characters`
  - 数据库 ID: `3ea1156e-cbaa-40ec-b85c-4b55be6ecc58`
  - 绑定变量名: `DB`

---

## 2. 部署流程 (唯一指定)

本项目的部署策略是“**阶段化提交**”。根目录为开发工作区，可以存放任意文件。`deploy/` 目录是**唯一**的生产环境部署源。

**Cloudflare 平台设置**:
- 必须在 Cloudflare Pages 的 `设置 -> 构建和部署` 中，将 **构建输出目录 (Build output directory)** 设置为 `/deploy`。
- **构建命令 (Build command)** 必须留空。

### 部署步骤

1.  **同步文件**: 将根目录的最新版核心文件，覆盖到 `deploy/` 目录中。
    ```powershell
    Copy-Item -Path "index.html", "script.js", "style.css" -Destination "deploy/" -Force
    ```
    *注意：`tags.json` 和 `characters.json` 等数据文件如果过大或不常变动，可以不每次都同步。*

2.  **提交更改**: 将对 `deploy/` 目录的修改提交到 Git。
    ```powershell
    git add deploy/
    git commit -m "feat: Sync latest changes to deploy directory"
    ```

3.  **推送部署**: 将 commit 推送到 `origin` 的 `main` 分支，即可触发 Cloudflare 的自动部署。
    ```powershell
    # 默认使用 7890 端口代理进行推送
    git -c http.proxy="http://127.0.0.1:7890" -c https.proxy="http://127.0.0.1:7890" push origin main
    ```
    > **⚠️ PowerShell 命令警告**: 在 PowerShell 终端中，**严禁使用 `&&`** 连接多个命令。请将命令分行执行，或使用分号 `;` (如果适用)。

---

## 2.x Git 仓库（仅用于代码版本管理）

### 2.x.1 仓库地址

| 远程名 | 地址 | 用途 |
|--------|------|------|
| `origin` | https://github.com/mhmoma/comfyui-Web.git | **唯一推送目标**，连接 Cloudflare Pages 自动部署 |

> **注意**：本地可能仍保留名为 `deploy` 的 git 远程（`arcane-survivors-kcn2id`），该仓库已用于其他项目，**禁止再向该远程推送** comfyui-web 代码。`deploy/` **目录**仍是 Cloudflare 构建输出目录，与上述 git 远程无关。

### 2.x.2 推送失败时的备用命令

若默认代理推送失败，可尝试绕过代理后**仅推 origin**：

```powershell
$env:ALL_PROXY=''; $env:HTTP_PROXY=''; $env:HTTPS_PROXY=''
git push origin main
```

---

## 3. D1 数据库操作

### 3.1 当前数据规模

- 系列 (series): 862 个
- 角色 (characters): 30,609 个（无上限）
- 数据来源: AnimaDex API (`animadex_raw.json` → `characters.json`)

### 3.2 数据库 Schema

两张表 `series` 和 `characters`，定义在 `schema.sql`：

- `series`: id (TEXT PK), name (TEXT), count (INTEGER)
- `characters`: id (AUTO), series_id (TEXT FK), trigger_text, name, thumb_url, count, lora_url

### 3.3 填充数据库（Seed）

**通过 HTTP API 分页填充**（因 Worker 单次调用 API 请求数有上限 ~1000）：

- **端点**: `POST /api/characters/seed?page=N`
- **认证**: 请求头 `x-admin-key: Tomkk520525`
- **ADMIN_KEY**: `Tomkk520525`（已配置在 Cloudflare Pages 环境变量中）
- **分页**: 每页 10 个系列，总共 87 页 (page=0 到 page=86)
- **page=0 时**: 自动创建表结构 + 清空旧数据 + 插入前 10 个系列
- **最后一页**: 返回 `"done": true`
- **注意**: 由于 characters.json 接近 10MB，部分页面可能返回 Cloudflare 1102 错误，重试即可

**填充命令（PowerShell）**：

```powershell
# 清除代理
$env:ALL_PROXY=''; $env:HTTP_PROXY=''; $env:HTTPS_PROXY=''

# 第一页（创建表 + 清空 + 插入）
curl.exe --max-time 120 -X POST "https://comfyui-web-89u.pages.dev/api/characters/seed?page=0" -H "x-admin-key: Tomkk520525"

# 批量执行剩余页（含自动重试）
foreach ($p in 1..86) {
    Write-Host "Seeding page $p..."
    for ($attempt = 1; $attempt -le 5; $attempt++) {
        $r = curl.exe --max-time 120 -s -X POST "https://comfyui-web-89u.pages.dev/api/characters/seed?page=$p" -H "x-admin-key: Tomkk520525"
        Write-Host $r
        if ($r -match '"ok"') { break }
        Write-Host "Retry $attempt for page $p..."
        Start-Sleep -Seconds ($attempt * 3)
    }
    Start-Sleep -Seconds 1
}
```

### 3.4 验证数据库

```
GET https://comfyui-web-89u.pages.dev/api/characters/series
→ 应返回 862 个系列的 JSON 数组

GET https://comfyui-web-89u.pages.dev/api/characters/search?q=ganyu&limit=5
→ 应返回甘雨角色数据（含 trigger、缩略图、LoRA）

GET https://comfyui-web-89u.pages.dev/api/characters/genshin_impact
→ 应返回原神所有角色
```

### 3.5 seed.js 的数据源

seed 函数从 GitHub Raw 获取 `characters.json`：
```
https://raw.githubusercontent.com/mhmoma/comfyui-Web/main/characters.json
```
所以 `characters.json` 必须存在于 origin 仓库的 main 分支中。

---

## 4. API 端点汇总

| 路径 | 方法 | 说明 | 认证 |
|------|------|------|------|
| `/api/characters/series` | GET | 获取所有系列列表 | 无 |
| `/api/characters/search?q=XXX&limit=N` | GET | 搜索角色 | 无 |
| `/api/characters/{seriesId}` | GET | 获取指定系列的所有角色 | 无 |
| `/api/characters/seed?page=N` | POST | 填充数据库（分页） | x-admin-key |
| `/api/nai/generate` | POST | NAI 生图 | 参见 env.NAI_API_KEY |
| `/api/nai/status` | GET | 生图状态 | 无 |

---

## 5. Cloudflare 环境变量

在 Cloudflare Pages → Settings → Environment variables 中配置：

| 变量名 | 值 | 说明 |
|--------|------|------|
| `ADMIN_KEY` | `Tomkk520525` | seed API 和管理接口认证 |
| `NAI_API_KEY` | (已配置) | NAI 生图 API 密钥 |

D1 绑定在 Settings → Functions → D1 database bindings：

| 变量名 | 数据库 |
|--------|--------|
| `DB` | comfyui-characters (3ea1156e-cbaa-40ec-b85c-4b55be6ecc58) |

---

## 6. 关键文件说明

| 文件 | 说明 |
|------|------|
| `wrangler.toml` | D1 绑定配置（本地开发用） |
| `schema.sql` | 数据库表结构 |
| `seed_d1.js` | 生成 seed.sql 的 Node 脚本 |
| `seed.sql` | 生成的种子数据 SQL |
| `build_characters.js` | 从 animadex_raw.json 构建 characters.json |
| `strip_chars_from_tags.js` | 从 tags.json 移除角色数据 |
| `fetch_animadex.js` | 从 AnimaDex API 拉取角色数据 |
| `characters.json` | 完整角色数据（862 系列 / 30,609 角色，~10MB） |
| `series_char_counts.json` | 各作品真实角色条数（862 条，~18KB，前端展示用） |
| `tags.json` | 标签数据（不含角色，416KB） |

---

## 7. 版本历史

| 版本 | 日期 | 操作 | 说明 |
|------|------|------|------|
| 1.01 | 2026-07-02 | 首次推送 + 填充 | D1 数据库整合完成，862 系列 / 12,460 角色写入，前端改为懒加载 |
| 1.02 | 2026-07-02 | 加入角色特征标签 | characters 表新增 tags 列，API 返回 tags，前端预览弹窗显示特征标签 + Trigger+Tags 按钮 |
| 1.03 | 2026-07-02 | 取消角色上限 + 重填数据库 | 角色数从 12,460 → 30,609（无上限），seed 批次改为每页 10 系列（共 87 页），填充脚本加自动重试 |
| 1.04 | 2026-07-03 | 调整UI | 角色标签预览图尺寸从 48px 增加到 64px |
| 1.05 | 2026-07-03 | 修复部署流程 | 废弃了错误的 Wrangler 手动部署方案，回归并明确了正确的 `/deploy` 目录部署策略。|
| 1.06 | 2026-07-03 | 终极规则修正 | 再次修正部署流程，并增加了 PowerShell 命令使用警告。 |
| 1.07 | 2026-07-03 | 修正代理规则 | 明确了 Git 推送必须通过 18888 端口代理，而非绕过。 |
| 1.08 | 2026-07-03 | 全量翻译作品名 | 翻译了所有作品系列名为中文，并更新了 `tags.json` 文件。同时修正推送代理端口为 7890。 |
| 1.09 | 2026-07-03 | IP-Adapter + 架构感知 | 集成 IP-Adapter 风格迁移面板（模型扫描/一键下载/workflow注入），新增架构感知系统（Anima模式自动禁用不兼容功能）。 |
| 1.10 | 2026-07-04 | 手机端适配优化 | 底部 Tab 导航（创作/设置/历史）、sticky 生成栏、结果区展开、历史触控优化、NAI 视频帧纵向布局等。 |
| 1.11 | 2026-07-04 | 手机端 C 方案 | 5 Tab（创作/角色/标签/设置/历史）、全屏角色浮层、FAB 生成、创作页精简。 |
| 1.12 | 2026-07-04 | 标签/角色 Tab 修复 | 标签 Tab 跳过人物大类；角色网格 2 列全身缩略图。 |
| 1.13 | 2026-07-04 | 角色浮层缩略图修复 | 独立 thumb 容器 + 样式表末尾强制 contain，修复全身图被裁成一条的问题。 |
| 1.14 | 2026-07-04 | 标签/画师 Tab 重构 | 标签 Tab 保留人物（仅对象标签）；新增画师 Tab + 底部分类切换（精选/热门/收藏/字母）。 |
| 1.15 | 2026-07-04 | 角色浮层卡片布局 | 竖向 3:4 缩略图 + 底部中英文名（与桌面端一致）。 |
| 1.16 | 2026-07-04 | 画师/角色浮层修复 | 画师 Tab 首次进入清空搜索；角色卡片去除 CSS 冲突、显示名字区。 |
| 1.17 | 2026-07-04 | 角色浮层独立卡片 | 脱离 tag-item 全局样式，padding 比例盒固定缩略图高度。 |
| 1.18 | 2026-07-04 | 角色浮层高度修复 | 修复 border-box 下缩略图高度塌缩；2 列可滚动每页 40 张完整卡片。 |
| 1.19 | 2026-07-04 | 角色浮层 flex 布局 | 弃用 Grid，改 flex 双列 + 固定 190px 缩略图高度，与桌面端一致。 |
| 1.20 | 2026-07-04 | 三列布局 + 画师 Tab 修复 | 角色/画师手机三列紧凑；修复首次进入画师 Tab 显示标签的问题。 |
| 1.21 | 2026-07-04 | 缩略图铺满卡片 | 角色/画师浮层改 object-fit:cover，去除两侧黑边。 |
| 1.22 | 2026-07-04 | 角色栏封面网格 | 作品列表改 3 列封面卡片（API 返回 cover_url）+ 最近横滑。 |
| 1.23 | 2026-07-04 | 作品分页 + 封面修复 | 6 列小图、每页 120 个、最近 8 个；修复封面懒加载与补全逻辑。 |
| 1.24 | 2026-07-04 | 标签栏本地缓存 | tags.json + 作品系列 stale-while-revalidate，启动不再阻塞 API。 |
| 1.25 | 2026-07-04 | IndexedDB 大缓存 | 标签/作品缓存迁 IndexedDB；标签栏不再等 ComfyUI。 |
| 1.26 | 2026-07-04 | 作品角色数修复 | 新增 series_char_counts.json，作品卡片显示真实角色条数而非 Danbooru 热度总和。 |
| 1.27 | 2026-07-05 | 架构切换状态隔离 | SDXL/Anima 切换时分别保存恢复提示词、采样器、调度器等参数，防止跨架构泄漏。 |
| 1.28 | 2026-07-06 | 教程资源下载 | 教程弹窗新增各模型介绍与 HuggingFace 一键下载链接。 |
| 1.29 | 2026-07-06 | 停用备用 git 远程 | 不再向 `deploy` 远程（arcane-survivors）推送；仅推 `origin`。 |
| 1.30 | 2026-07-06 | 常用标签角标防混淆 | 标签使用角标从 `×N` 改为 `N次`，且仅在「最近/常用」页显示，避免被误认成提示词权重。 |
| 1.31 | 2026-07-06 | 生成状态与采样预览 | 新增连接状态灯、生成耗时/进度同步（标题/FAB），并支持 ComfyUI WebSocket 预览帧实时采样预览（无帧自动降级）。 |
| 1.32 | 2026-07-06 | 预览与进度修正 | 预览改为绑定 `client_id` 并解析 ComfyUI 二进制预览帧头；进度条改为只增不减，轮询仅做兜底，修复跳动与预览无效。 |
| 1.33 | 2026-07-06 | 后处理前底图预览 | 开启高清放大/面部修复时可先预览底图，再选择进入后处理、重抽底图或结束生图。 |
| 1.34 | 2026-07-06 | 配置方案持久化 | 本地保存多套生成参数方案，支持自动保存、下拉切换、另存为/重命名/删除。 |
| 1.35 | 2026-07-06 | 后处理底图上传修复 | 进入后处理前将 output 底图重新上传到 input，修复 LoadImage 400 错误。 |
| 1.36 | 2026-07-06 | 配置方案保存逻辑 | 方案仅手动保存时更新；日常修改写入会话状态，切换方案不再覆盖已存方案。 |
| 1.37 | 2026-07-06 | 架构与方案隔离 | SDXL/Anima 分别保存 LoRA、后处理等模块；切换架构或方案时完整恢复互不影响。 |
| 1.38 | 2026-07-06 | 局部重绘与预设 | 结果图蒙版局部重绘，含换衣服/换背景/擦除/修细节/换发型等场景预设与推荐强度。 |
| 1.39 | 2026-07-07 | 局部重绘笔刷修复 | 修复笔刷不显示涂抹：弹窗先显示再初始化画布，画布与图片对齐，布局未就绪时自动重试。 |
| 1.40 | 2026-07-07 | 局部重绘多入口 | 历史记录、大图预览、img2img/ControlNet/IPA 上传预览均可进入局部重绘。 |
| 1.41 | 2026-07-07 | 局部重绘蒙版显示修复 | 修复打开即全图发红、画笔橡皮无效；蒙版按亮度显示，增加圆形笔刷光标。 |
| 1.42 | 2026-07-07 | 全屏蒙版工作台 | 全屏局部重绘、缩放平移、反选蒙版、魔棒选区、1px像素笔。 |
| 1.43 | 2026-07-07 | 后处理底图上传加固 | 底图预览后重新上传 input，带重试与 canvas 回退，修复 CW_Base LoadImage 400。 |
| 1.44 | 2026-07-07 | 局部重绘笔刷坐标修复 | 笔刷圈移出缩放层、反算 pan/scale 坐标，修复错位与需点清空才生效。 |
| 1.45 | 2026-07-07 | 后处理上传校验修复 | 修复 cw_base 上传成功却被 CW_Base 误判拦截导致后处理中断。 |
| 1.46 | 2026-07-07 | 局部重绘蒙版类型修复 | ImageToMask 转换蒙版 + grow_mask_by，修复 VAEEncodeForInpaint 400 校验失败。 |
| 1.47 | 2026-07-07 | 局部重绘工作流修正 | 改用 VAEEncode + SetLatentNoiseMask，修复普通模型重绘出灰块。 |
| 1.48 | 2026-07-07 | 局部重绘独立模型设置 | 重绘固定 SDXL 专用模型/采样参数，与 Anima 主架构解耦。 |
| 1.49 | 2026-07-07 | 局部重绘成人预设 | 裸体/去毛/私处/胸/内衣等即用预设，提示词已填好。 |
| 1.50 | 2026-07-07 | 局部重绘左侧设置栏 | 模型/采样/提示词改左侧竖排，可滑出屏幕外折叠。 |
| 1.51 | 2026-07-07 | 局部重绘工作流加固 | InpaintModelConditioning + 底图蒙版同尺寸导出，修复重绘无效。 |
| 1.52 | 2026-07-07 | Inpaint noise_mask 修复 | InpaintModelConditioning 补 noise_mask:true，修复 400 校验失败。 |
| 1.53 | 2026-07-07 | 局部重绘贴合原图 | 降低去衣强度、自动一致词、贴合原图开关与强度上限 0.68。 |
| 1.54 | 2026-07-07 | 蒙版内保留原图潜变量 | 贴合原图改 VAEEncode+SetLatentNoiseMask，避免蒙版区整段重画。 |
| 1.55 | 2026-07-07 | 局部重绘恢复生效 | 统一 InpaintModelConditioning；LoadImageMask；导出顺序与强度下限修复。 |
| 1.56 | 2026-07-07 | Fooocus Inpaint 引擎 | 自动检测 Fooocus/Differential Diffusion；三档模式；合成保原图；一键下载补丁。 |
| 1.57 | 2026-07-07 | Anima LLLite 局部重绘 | Anima 模式接入 LLLite+InpaintPreprocessor；裁剪拼接小范围；一键下载 LLLite 权重。 |
| 1.58 | 2026-07-07 | 统一推送代理端口 | 部署文档默认 Git 推送代理统一为 `7890`，保留失败时绕过代理仅推 `origin` 的备用方案。 |
| 1.59 | 2026-07-07 | 补齐前端版本号同步 | 修正 `index.html` 与 `deploy/index.html` 前端版本到 `v3.91`，并统一静态资源版本参数避免缓存错配。 |
| 1.60 | 2026-07-07 | 修复 Anima 裁剪节点参数校验 | 局部重绘动态读取 `InpaintCropImproved` 必填 schema 并自动补齐参数，兼容插件字段变更导致的 400 校验失败。 |
| 1.61 | 2026-07-07 | 前端版本递增规范补齐 | 将前端版本递增到 `v3.92`（含 `script.js?v=388`），确保每次有效更新均递增版本号。 |
| 1.62 | 2026-07-07 | 强化版本号刚性规则 | 明确前端相关代码每次变更都必须递增前端版本与资源版本参数，禁止复用旧版本号。 |
| 1.63 | 2026-07-07 | 对齐 Anima v22 默认采样参数 | Anima 局部重绘在 FLS 路径默认对齐 `v22`（12 步、CFG 1、euler、simple），并更新前端版本到 `v3.93`。 |
| 1.64 | 2026-07-07 | Anima 局部重绘锁定 v22 默认 | 关闭局部重绘配置持久化；Anima 固定 v22 参数（含 denoise=1、simple 调度），并锁定步数/CFG/调度/强度仅保留采样器可调。 |
| 1.65 | 2026-07-07 | 替换为两套 Anima 重绘方案 | 下线旧 Anima 局部重绘模式，严格按工作流提供“强化小范围 / 大范围重绘”两套参数方案。 |
| 1.66 | 2026-07-07 | 局部重绘改为 Anima-only | 下线 SDXL 局部重绘入口与执行路径，局部重绘仅保留 Anima 工作流两套方案。 |
| 1.67 | 2026-07-07 | 清理遗留 SDXL/旧引擎代码 | 删除 SDXL 局部重绘与 Fooocus/Diffusion 等不可达分支代码，保持 Anima-only 流程纯净。 |
| 1.68 | 2026-07-07 | 方案切换联动 UI 参数 | 切换“强化小范围/大范围重绘”时，步数/CFG/采样器等展示值同步切换到对应方案，避免前端显示与实际工作流不一致。 |
| 1.69 | 2026-07-07 | 固定反向提示词并隐藏输入 | Anima 局部重绘反向提示词使用工作流固定文本，前端不显示也不允许修改，仅保留正向输入。 |
| 1.70 | 2026-07-07 | 新增简化中文提示词预设 | 局部重绘新增简化预设词条（中文提示 + 英文写入），保持反向固定且仅允许正向输入。 |
| 1.71 | 2026-07-07 | 精简局部重绘参数区 | 局部重绘仅保留可调“采样器/调度器”；隐藏不可调参数与 Anima 说明文案，仅显示引擎检查状态。 |
| 1.72 | 2026-07-07 | 安装脚本兼容性增强 | 一键检查/安装脚本新增 Git 缺失时 ZIP 回退安装、生成缺失报告文件，并加固 BAT 对 PowerShell 路径检测，避免双击闪退与命令不可用报错。 |
| 1.73 | 2026-07-07 | 兼容启动器目录结构 | 安装脚本支持在启动器目录自动识别 `ComfyUI` 子目录为根路径，避免误报“不在根目录”并可正常完成检查。 |
| 1.74 | 2026-07-07 | 局部重绘大小范围链路对齐 | 小范围改裁剪拼接+InpaintStitch，大范围改全图+MaskFix+；采样参数对齐用户工作流，并保证 UNET→LLLite→FLS 模型链完整。 |
| 1.75 | 2026-07-07 | FLS 调度器兼容回退 | 构建局部重绘前读取 FLS_SamplerV4 可用 scheduler/sampler 列表，beta57 等不支持值自动回退，避免 400 校验失败。 |
| 1.76 | 2026-07-07 | 锁定工作流预设参数 | 局部重绘两套方案严格按用户导出 JSON 填参，移除 scheduler/sampler 自动改写与 UI 覆盖。 |
| 1.77 | 2026-07-07 | FLS 调度器不可用回退默认 | 小范围优先 beta57；当前 FLS_SamplerV4 不支持时改用该节点 object_info 默认 scheduler。 |
| 1.78 | 2026-07-07 | 局部重绘采样调度可编辑 | 小范围默认 scheduler 改为 simple；采样器/调度器可在面板修改并用于构建工作流。 |
| 1.79 | 2026-07-07 | 安装脚本对齐新版重绘节点 | 一键安装加入 FLS_Sampler、CropAndStitch、essentials；移除 Fooocus 旧依赖。 |
| 1.80 | 2026-07-07 | 同步线上前端版本号 | `deploy/index.html` 版本号同步到 `v4.07`（`script.js?v=402`），保证线上显示与本次发布一致。 |
| 1.81 | 2026-07-07 | 图层合成工具 | 新增全屏 A 叠 B 合成：拖拽/缩放/旋转/透明度/对齐线，支持历史与结果图一键导入，纯前端线上可用（`v4.08` / `script.js?v=404`）。 |
| 1.82 | 2026-07-07 | 浏览器抠图 + 合成串联 | 集成 `@imgly/background-removal` 本地抠图，支持历史/结果/合成 A 层入口，抠完可送入图层合成（`v4.09` / `script.js?v=405`）。 |
| 1.84 | 2026-07-08 | 手机端整屏适配 | 修复横向溢出需左右拖动：顶栏双行布局、禁止页面级横向滚动、全屏 dvh 壳层、内容区宽度约束（`v4.11` / `style.css?v=397` / `script.js?v=407`）。 |
| 1.85 | 2026-07-08 | 结果/历史面板交互增强 | 生成结果区改为可滚动并在生成完成后自动定位到底部；历史面板支持折叠/隐藏、可拖拽拉宽、上下滚动与更紧凑缩略图展示（`v4.12` / `style.css?v=398` / `script.js?v=408`）。 |
| 1.86 | 2026-07-08 | 历史面板交互兼容修正 | 平板（<=1000px）下隐藏历史面板 resizer/折叠控件，避免与竖向布局冲突；JS 在 <=1000px 不启用折叠/拖拽（`v4.13` / `style.css?v=399` / `script.js?v=409`）。 |
| 1.87 | 2026-07-08 | 结果预览/滚动/历史面板再次修正 | 预览图高度上限收小；点击生成立即滚到底；历史拖拽方向修正；历史面板折叠改为隐藏整个栏向屏幕外挪出，且缩略图大小更接近原先（`v4.14` / `style.css?v=400` / `script.js?v=410`）。 |
| 1.88 | 2026-07-08 | 固定结果界面并强化“点击生成即滚到底” | 移动端点击生成后立即进入与生成后同样的固定结果界面大小（不再因未生成状态导致界面偏小）；滚动定位改为按元素 offset 计算更稳定；历史面板折叠向屏幕外方向修正（`v4.15` / `style.css?v=401` / `script.js?v=411`）。 |
| 1.89 | 2026-07-08 | 生成开始时机滚动兜底 | 在 `beginGenerationUI` / 工作流生成起始阶段再次执行“滚动到结果区底部”，解决点击生成后偶发未滚到底的问题（`v4.16` / `style.css?v=402` / `script.js?v=412`）。 |
| 1.90 | 2026-07-08 | 固定结果舞台与历史外侧收起 | 未生图前也保持固定结果舞台尺寸；点击生成立即跳到结果舞台；历史记录取消 50 条上限可持续滚动；历史栏收起改为向屏幕外滑出并释放主界面宽度（`v4.17` / `style.css?v=403` / `script.js?v=413`）。 |
| 1.91 | 2026-07-08 | 生成提示改悬浮不占位 | 将结果区“正在生成中”提示改为绝对定位浮层，不再在顶部占据大块空白并挤压预览图（`v4.18` / `style.css?v=404` / `script.js?v=414`）。 |
| 1.83 | 2026-07-07 | 抠图边缘可调节 | 抠图后支持边缘收缩/透明阈值/羽化滑块，本地即时重算无需重跑 AI（`v4.10` / `script.js?v=406`）。 |
